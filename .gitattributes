# Normalize line endings. Shell scripts MUST stay LF or they break in Linux
# containers ("bad interpreter"), even when cloned on Windows.
* text=auto
*.sh text eol=lf
Dockerfile text eol=lf
docker-entrypoint.sh text eol=lf
