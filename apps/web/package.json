{
  "name": "@lyricsearch/web",
  "version": "0.0.0",
  "private": true,
  "description": "Hosted service — multi-tenant, Postgres. Shares @lyricsearch/core with the Personal Edition.",
  "license": "UNLICENSED",
  "type": "commonjs",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "migrate": "node src/migrate.js",
    "test": "node --test",
    "start": "node src/server.js",
    "worker": "node src/worker.js"
  },
  "dependencies": {
    "@lyricsearch/core": "*",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "nodemailer": "^9.0.3",
    "pg": "^8.13.1",
    "pg-boss": "^12.26.3"
  }
}
