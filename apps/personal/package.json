{
  "name": "@lyricsearch/personal",
  "version": "1.0.0",
  "description": "Personal Edition — search your Spotify export by lyrics and build playlists from the matches. Self-hosted, single-user, SQLite.",
  "license": "ISC",
  "type": "commonjs",
  "main": "src/server.js",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "ingest": "node src/ingest.js",
    "lyrics": "node src/lyrics.js",
    "setup": "node src/ingest.js && node src/lyrics.js",
    "test": "node --test"
  },
  "dependencies": {
    "@lyricsearch/core": "*",
    "dotenv": "^17.4.2",
    "ejs": "^6.0.1",
    "express": "^5.2.1"
  }
}
